#include <iostream>
#include <string>

// /* The << operator writes its second argument onto its first, so "Hello, World" onto the output stream cout. */
int main() {
    std::cout << "Hello, World!\n";
    std::string s;

    std::cin >> s;
    return 0;
}