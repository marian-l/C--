//
// Created by maria on 18.09.2023.
//
#include <iostream>
#include "LeetCode.h"

int main() {
    LeetCode leetCode = LeetCode();

    // std::vector<int> nums = {1,3,5,6};
    // int result = leetCode.searchInsert(nums, 2);
    // std::cout << result;

    // std::vector<int> result = leetCode.searchRange(nums, 5);
    // std::cout << result[0] << " : " << result[1];

    // std::cout << leetCode.mySqrt(36);

    std::vector<std::vector<int>> matrix = {{1}};

    for (int i = 0; i < ; ++i) {
        
    }
    
    std::cout << leetCode.searchMatrix(matrix, 0);
        return 0;
        
}